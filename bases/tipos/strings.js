"use strict";
(() => {
    var _a, _b;
    console.log('Strings');
    const batman = 'Batman';
    const linternaVerde = "Linterna's Verde";
    const volcanNegro = `Volcán Negro ${batman}`;
    const abc = 123;
    console.log(` I'm ${batman}, abc`);
    console.log(batman.toUpperCase());
    console.log((_b = (_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) !== null && _b !== void 0 ? _b : 'No hay caracter');
})();
